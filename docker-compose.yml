services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./charts:/app/charts
    environment:
      - GEONAMES_USERNAME=${GEONAMES_USERNAME}
      - POCKETBASE_EMAIL=${POCKETBASE_EMAIL}
      - POCKETBASE_PASSWORD=${POCKETBASE_PASSWORD}
      - ASTRONOMY_APP_ID=${ASTRONOMY_APP_ID}
      - ASTRONOMY_APP_SECRET=${ASTRONOMY_APP_SECRET}
      # SSL verification for geocoder service (false = disabled for Coolify internal certs)
      - GEOCODER_VERIFY_SSL=false
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
